# {{ package_name }}

ROS2 vision node running **{{ model_name }}** ({{ variant }}) on **{{ backend }}**.

Generated by [vision-factory](https://github.com/robocore/vision-factory).

## Quick Start

```bash
# Install Python dependencies
pip install -r requirements.txt

# Build with colcon
cd /path/to/ros_ws/src
cp -r {{ package_name }} .
cd ..
colcon build --packages-select {{ package_name }}
source install/setup.bash

# Run
ros2 launch {{ package_name }} vision.launch.py
```

## Docker

```bash
docker build -t {{ package_name }} .
docker run --gpus all --rm {{ package_name }}
```

## Configuration

Edit `config/params.yaml`:

```yaml
{{ node_name }}:
  ros__parameters:
    input_topic: "/camera/image_raw"
{% for param in parameters %}
    {{ param.name }}: {{ param.default | yaml_repr }}  # {{ param.description }}
{% endfor %}
```

## Topics

### Subscribed
- `input_topic` (sensor_msgs/Image) — Input camera image
{% if has_text_input %}
- `prompt` (std_msgs/String) — Text prompt
{% endif %}

### Published
{% for pub in publishers %}
- `{{ pub.topic }}` ({{ pub.msg_type }}){% if pub.description %} — {{ pub.description }}{% endif %}

{% endfor %}

## Fine-tuned Models

To use your own fine-tuned weights, update `config/params.yaml`:

```yaml
{{ node_name }}:
  ros__parameters:
    # Point to your custom weights:
    # weights_path: "/path/to/my_finetuned_model.pt"
```

The architecture code is identical — only the weights change.
